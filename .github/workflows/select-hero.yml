name: Select Daily Hero Story

on:
  schedule:
    - cron: '0 2 * * *'  # 02:00 UTC = 04:00 CEST — muss VOR Newsletter (04:20 UTC) fertig sein
  workflow_dispatch:

jobs:
  select-hero:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests python-dotenv

      - name: Select hero story
        run: python scripts/select_hero.py
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
